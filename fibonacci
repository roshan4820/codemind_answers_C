#include<stdio.h>
int main(){
    int n,j,i,f1=0,f2=1,fib,temp;
    scanf("%d",&n);
    for(i=0;i<n;i++){
        printf("%d ",f1);
        temp=f1+f2;
        f1=f2;
        f2=temp;
    }
    return 0;
}
